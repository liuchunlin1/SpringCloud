server:
  port: 9090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liuchunlin1/cloud-config.git
          search-paths: cloud-config/config
  application:
    name: config-server